app: domain-robot

component: layer
name: domain-robot-layer

inputs:
  region: ${env:REGION}
  name: ${name}
  src:
    src: ../node_modules
    targetDir: node_modules
  runtimes:
    - Nodejs12.16
